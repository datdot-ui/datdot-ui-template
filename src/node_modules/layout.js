const bel = require('bel')
module.exports = layout
function layout(opts) {
    const { name, className, content } = opts
    const element = bel`<section role=${name} class=${className} aria-label=${name}>${content}</section>`
    return element
}